# 📚 Telegram Vocabulary Bot с AI

Персональный словарь с AI-помощником на базе Google Gemini для изучения новых слов.

## ✨ Возможности

- 🤖 **AI объяснения** - Gemini подробно объясняет значение слов
- 💾 **Личный словарь** - Сохранение слов с определениями
- 🎲 **Повторение** - Случайные слова для тренировки памяти
- 📊 **Статистика** - Отслеживание прогресса
- 🔍 **Поиск** - Найти нужное слово в своем словаре
- 📱 **Telegram** - Удобный доступ из любого места

## 🚀 Быстрый старт

### 1. Установка

```bash
# Клонируй репозиторий
git clone <your-repo-url>
cd telegram_vocabulary_bot

# Создай виртуальное окружение
python -m venv venv

# Активируй его
# На Windows:
venv\Scripts\activate
# На Mac/Linux:
source venv/bin/activate

# Установи зависимости
pip install -r requirements.txt
```

### 2. Получение API ключей

#### Telegram Bot Token

1. Открой Telegram и найди [@BotFather](https://t.me/BotFather)
2. Отправь `/newbot`
3. Введи имя и username для бота
4. Скопируй полученный токен

#### Gemini API Key

1. Перейди на [Google AI Studio](https://makersuite.google.com/app/apikey)
2. Нажми "Create API Key"
3. Скопируй ключ

### 3. Настройка

```bash
# Скопируй example файл
cp .env.example .env

# Открой .env и вставь свои ключи
nano .env  # или любой другой редактор
```

Твой `.env` должен выглядеть так:

```
TELEGRAM_BOT_TOKEN=7234567890:AAHdqTcvCH1vGWJxfSeofSAs0K5PALDsaw
GEMINI_API_KEY=AIzaSyB1234567890abcdefghijklmnop
```

### 4. Запуск

```bash
python main.py
```

Бот запущен! 🎉

## 📖 Как использовать

### Основные команды

- `/start` - Начать работу с ботом
- `/help` - Помощь
- `/dictionary` - Показать все сохраненные слова
- `/random` - Получить случайное слово для повторения
- `/stats` - Твоя статистика

### Добавление слов

1. Просто отправь слово боту: `синтезировать`
2. Получи подробное объяснение от AI
3. Нажми кнопку "💾 Сохранить" если хочешь добавить в словарь

## 🗄️ База данных

По умолчанию используется **SQLite** - не требует установки, файл `vocabulary.db` создается автоматически.

### Миграция на PostgreSQL (для продакшена)

```bash
# Установи дополнительную зависимость
pip install psycopg2-binary

# Добавь в .env:
DATABASE_URL=postgresql://user:password@localhost:5432/vocabulary_db
```

Затем в `main.py` измени инициализацию:

```python
from database import PostgresDatabase
db = PostgresDatabase(os.getenv('DATABASE_URL'))
```

## ☁️ Деплой (бесплатные варианты)

### Railway.app

1. Зарегистрируйся на [railway.app](https://railway.app)
2. Подключи свой GitHub репозиторий
3. Добавь переменные окружения (TELEGRAM_BOT_TOKEN, GEMINI_API_KEY)
4. Deploy!

### Render.com

1. Зарегистрируйся на [render.com](https://render.com)
2. Создай новый "Background Worker"
3. Подключи репозиторий
4. Команда запуска: `python main.py`
5. Добавь переменные окружения
6. Deploy!

### Fly.io

```bash
# Установи flyctl
curl -L https://fly.io/install.sh | sh

# Залогинься
flyctl auth login

# Запусти деплой
flyctl launch

# Установи секреты
flyctl secrets set TELEGRAM_BOT_TOKEN=your_token
flyctl secrets set GEMINI_API_KEY=your_key

# Deploy
flyctl deploy
```

## 🔧 Расширение функционала

### Добавить категории/теги

В `database.py` добавь поле `category`:

```python
cursor.execute("""
    CREATE TABLE IF NOT EXISTS words (
        id INTEGER PRIMARY KEY AUTOINCREMENT,
        user_id INTEGER NOT NULL,
        word TEXT NOT NULL,
        definition TEXT NOT NULL,
        category TEXT,  # <-- новое поле
        created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
    )
""")
```

### Добавить экспорт в PDF/Excel

```bash
pip install fpdf openpyxl
```

### Добавить напоминания

Используй `APScheduler`:

```bash
pip install apscheduler
```

## 🤝 Вклад

Нашел баг или есть идея? Создай Issue или Pull Request!

## 📄 Лицензия

MIT License - делай что хочешь!

## 🙋 Вопросы?

Открывай Issue в репозитории или пиши в Telegram.

---

**Сделано с ❤️ для изучения языков**
